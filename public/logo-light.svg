<!-- PLACEHOLDER wordmark, not the HIG logo mark.
     Replace this file and logo-dark.svg with the real artwork; keep the same
     paths and a similar aspect ratio and nothing in the app needs changing.
     Then run `npm run icons` to regenerate the home-screen icons from it. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 112 32" width="112" height="32">
  <text x="0" y="25" font-family="Helvetica, Arial, sans-serif" font-weight="700"
        font-size="27" letter-spacing="0.5">
    <tspan fill="#1975bf">HI</tspan><tspan fill="#3ab54a">G</tspan>
  </text>
</svg>
