# Supabase project: HIG Biz Operation — the same project the web app uses.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Secrets. Never NEXT_PUBLIC_, and never read outside a request handler.
# From @BotFather, for the bot that hosts the mini app.
TELEGRAM_BOT_TOKEN=
# Supabase dashboard -> Project Settings -> API keys -> secret (sb_secret_...).
SUPABASE_SECRET_KEY=
